# Write a Python program to:

# Take a string input from the user.
# Print the length of the string.
# Convert the string to uppercase and lowercase.


just = str(input("Enter the any word that you want: "))

print(f"The length of {just} word is {len(just)}")
print(f"The uppercase version of {just} is that: {just.upper()}")